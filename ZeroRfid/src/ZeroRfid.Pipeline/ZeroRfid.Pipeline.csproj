<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>netstandard2.0;net462;net8.0</TargetFrameworks>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <Description>High-throughput RFID streaming pipeline engines: sliding-window tag deduplication, RSSI signal filtering, and real-time portal gate vector direction detection for ZeroPlatform.</Description>
    <PackageTags>rfid;pipeline;deduplication;debounce;gate-direction;portal;rssi;zeroplatform</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\ZeroRfid.Core\ZeroRfid.Core.csproj" />
  </ItemGroup>
</Project>
